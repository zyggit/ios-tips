1，遇到签名和配置文件不匹配，但是多次下载最新的证书和配置文件都不能解决变异错误；最后只能把文件夹下所有的配置文件都删除了，然后重新生成；
2,有关与结构库指令集的问题：
    Architecture：选择自己想要支持的指令集（也就是要在哪些系统上高效的运行）
    Build Active Architecture Only ：之编译当前的一个指令集（只生成在当前的系统下的ipa）
                                    选择NO 就是编译你选中的即将要编译指令集合里的所有框架
                                    选择YES 就生成一个指令集
    Valid architectures：即将编译的指令集，将要加进去的指令集；
    一般的：debug选择yes release选择no
    
    
3，有关证书问题
    在本机钥匙串访问中向苹果请求CertificateSigningRequest.certSigningRequest 电脑相关的签名证书时，常用名称处填写的内容，将成为
    私有密钥的名称；每台计算机请求下来的证书都会带有一个termid；建议在一台机器上请求生成开发证书之后，其他的电脑可以直接使用
    生成的.p12文件
    
